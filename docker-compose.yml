services:
    frontend:
        build:
            context: .
        container_name: tasks-frontend
        restart: unless-stopped
        ports:
            - "5173:5173"
        environment:
            VITE_API_URL: http://localhost:3001